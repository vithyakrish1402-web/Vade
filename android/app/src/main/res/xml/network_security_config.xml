<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration: Cleartext HTTP/WS is strictly forbidden in production -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development/Debug overrides: Allows cleartext communication with local development servers on loopback -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
